#include "bits/stdc++.h"
#define int long long
using namespace std;

using pii = pair<int, int>;

constexpr int N = 1E6 + 10, inf = 2E18 + 10;

void Gura(int &_turn) 
{
    
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr), cout.tie(nullptr);
    cout << fixed << setprecision(10);

    int Gawr = 1;
    // cin >> Gawr;
    for (int _i = 1; _i <= Gawr; _i++) {
        Gura(_i);
    }

    return 0;
}
